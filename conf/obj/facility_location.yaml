objective: 'facility_location'

benchmark:
  # b: upper bound of the lattice domain
  # r: cardinality constraint
  # It must hold that b <= r and b * r < n
  br:
    -
      - 3
      - 3
    -
      - 3
      - 6
    -
      - 3
      - 9
